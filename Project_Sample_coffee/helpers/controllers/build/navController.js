// Generated by CoffeeScript 1.10.0
(function() {
  angular.module('myApp').controller('navController', [
    '$scope', '$state', '$location', 'authenticateService', 'sessionService', function($scope, $state, $location, authenticateService, sessionService) {
      $scope.tabs = [
        {
          heading: 'home',
          route: 'home',
          active: true,
          isVisible: 'false'
        }, {
          heading: 'signin',
          route: 'signin',
          active: false,
          isVisible: 'false'
        }, {
          heading: 'signout',
          route: 'signout',
          active: false,
          isVisible: 'false'
        }, {
          heading: 'dashboard',
          route: 'dashboard',
          active: false,
          isVisible: 'false'
        }
      ];
      $scope.tabSelected = function(route) {
        $state.go(route);
      };
      $scope.showMenue = function(tab) {
        if (authenticateService.isLogged() === true && tab.heading === 'signin') {
          return false;
        } else if (authenticateService.isLogged() === false && tab.heading === 'signout') {
          return false;
        } else if (tab.heading === 'home' || tab.heading === 'dashboard') {
          return true;
        } else {
          return true;
        }
      };
    }
  ]);

}).call(this);
